# Specimen — Complete Copywriting
**Tone:** Brutally honest. Dry. Rebel nerd typo academia.
**Language:** English dominant.
**Handle:** @getspecimen
**Date:** February 27, 2026

---

## 1. UI — Search Input
`src/components/ui/SakaSearchInput.tsx`

| Element | Copy |
|---------|------|
| **Placeholder** | `drop the URL. we know why you're here.` |
| **Brand Name** | `Specimen` |
| **Brand Subtitle** | `Saka Studio & Engineering` |

---

## 2. UI — Buttons
`src/components/ui/AnalysisDashboard.tsx`

| Button | Copy |
|--------|------|
| **Analyze** | `read it.` |
| **Extract / Download** | `get it.` |
| **Reset** | `start over.` |

---

## 3. UI — Dashboard
`src/components/ui/AnalysisDashboard.tsx`

| Element | Copy |
|---------|------|
| **Section Label** | `Foundry` |
| **Download Progress** | `pulling {current} of {total}.` |
| **Font List Header** | `{count} assets identified.` |
| **Terminal Header** | `Log` |
| **Empty Log** | `idle.` |
| **Brand Footer Name** | `Specimen` |
| **Brand Footer Subtitle** | `Saka Studio & Engineering` |

---

## 4. Toast / Notification Messages
`src/components/ui/BrutalistNotification.tsx` & `src/app/page.tsx`

### Analyzing Sequence
```
reading.
finding the gaps.
there it is.
almost.
```

### Error Messages
| Kondisi | Copy |
|---------|------|
| **No URL** | `drop something first.` |
| **Failed** | `couldn't get in. {error}` |

---

## 5. Activity Log Messages
`src/app/page.tsx`

| State | Format |
|-------|--------|
| **Foundry identified** | `→ {foundryName}. {n} assets on the table.` |
| **Job started** | `→ running.` |
| **Batch mode** | `→ {n} assets. batch mode.` |
| **Intercept mode** | `→ intercept mode.` |
| **Complete** | `→ done.` |
| **Error** | `✕ {error message}` |

---

## 6. Download Filename
`src/app/page.tsx:187, 242`

```
specimen-{foundryName}-fonts.zip
```

---

## 7. Console Graffiti
`src/components/ui/ConsoleGraffiti.tsx`

```
nothing here worth taking.
everything worth taking is in the UI.

Specimen — Saka Studio & Engineering.
```

---

## 8. API & Server Messages
`src/app/api/font-download/route.ts` & `src/lib/server/font-downloader.ts`

| Kondisi | Copy |
|---------|------|
| `body bukan JSON` | `Request body must be a JSON object.` |
| `mode invalid` | `Invalid \`mode\` field.` |
| `css-url missing cssUrl` | `` `css-url` mode requires `cssUrl`. `` |
| `api-json missing apiUrl` | `` `api-json` mode requires `apiUrl`. `` |
| `direct-url missing fileUrl` | `` `direct-url` mode requires `fileUrl`. `` |
| `batch-direct missing fonts` | `` `batch-direct` mode requires a non-empty `fonts` array. `` |
| `browser-intercept missing targetUrl` | `` `browser-intercept` mode requires `targetUrl`. `` |
| `mode tidak didukung` | `Unsupported download mode.` |
| `field bukan URL valid` | `` Field `{field}` must be a valid URL. `` |
| `tidak ada font di CSS` | `No font URLs found in CSS.` |
| `path tidak menghasilkan array/object` | `` Path `{path}` must resolve to an array or object. `` |
| `request gagal` | `Request failed ({status}): {sourceUrl}` |

---

## 9. Digital Presence

### X / Twitter Bio
```
we extract fonts from type foundries.
drop the URL. we know why you're here.

by @sakaengineering
```

### Instagram Bio
```
specimen.
built by Saka Studio & Engineering.

drop the URL. we know why you're here.
getspecimen.xyz
```

### GitHub — Repo Description
```
we know why you're here.
```

### GitHub — Org Bio
```
we build Specimen.
drop the URL. we know why you're here.
```

---

## 10. Press / Media Kit

### Official One-liner
```
Specimen is a font extraction engine built by Saka Studio & Engineering. 
it reads type foundry pages, captures font assets, and delivers them as a complete set.
```

---

## 11. Email Accounts

| Purpose | Email |
|---------|-------|
| **Primary / Public** | `getspecimen@gmail.com` |
| **Press** | `specimenpress@gmail.com` |
| **Legal** | `specimenlegal@gmail.com` |

---

*Specimen Copywriting — Saka Studio & Engineering, 2026*