"use client";

/**
 * DesktopAmbientOverlay
 *
 * Placeholder preserved as a shell attachment point. The canonical
 * Windows 95 desktop is a solid teal field with an ordered 2x2 dither
 * pattern (already provided by the `.win-desktop` utility in globals.css).
 * No scanlines, no bloom, no pulsing overlays — per AGENTS.md Section
 * XVI.3 (prohibited motion patterns) and the Calm Technology doctrine.
 *
 * This component intentionally renders nothing. It remains exported so
 * existing call sites compile unchanged; when Specimen 95 earns a
 * material atmospheric layer in a future phase, it will belong here.
 */
export default function DesktopAmbientOverlay() {
  return null;
}
